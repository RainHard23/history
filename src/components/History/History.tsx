import React from 'react';
import {mockDataType} from "../../data/dataHistory";

type Props = {
    historyData: mockDataType[]
}
const History = ({historyData}: Props) => {
    return (
        <div>

        </div>
    );
};

export default History;